import { UserTokenDialog } from "../UserTokenDialog";

export default function UserTokenDialogExample() {
  return (
    <div className="p-8 flex items-center justify-center">
      <UserTokenDialog />
    </div>
  );
}
